[build-system]
requires = ["setuptools","setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "Flask-B3Jobs"
version = "0.0.1"
readme = "README.md"
dependencies = [
  'Flask-IAM>=0.0.1',
  'Flask-Apium>=0.0.1',
  'lostdata',
  'docker'
]
